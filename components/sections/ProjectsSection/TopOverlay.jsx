import React from 'react';
import s from './TopOverlay.module.scss';

const TopOverlay = () => {
    return (
        <div className={s.wrap}>
            
        </div>
    );
};

export default TopOverlay;